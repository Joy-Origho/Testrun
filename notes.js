'some' + ' ' + 'time'
'$' + (5698 + 345) / 100
// to calculate 2 items  in a category or cart using concatenation JS
'items + (' + ( 1 + 1) + '): $' + (2095 + 799) / 100
alert ('items + (' + ( 1 + 1) + '): $' + (2095 + 799) / 100);
//escape character using \n (newline xter) or \'
'I\'m happy today' 
'I am happy today\n am I' 

//using interpolation and template sting or backticks ` ` instead of concatenaton when you want to insert a value into a string. 
//dot forget to use the $ sign for backticks
//example, 
`items (${1 + 1}) : $${(2095 +799) / 100} `
//Using multi line sting instead of \n to create newline.
`some
text`
// Rules for re-assigning variable: Dont us let again cos this will just create a new variable again.